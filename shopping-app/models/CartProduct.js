export default class CartProduct{
 
    constructor(id, title, imageUrl,total){
            this.id = id;
            this.title = title;
            this.imageUrl = imageUrl;
            this.total=total;
    }
}